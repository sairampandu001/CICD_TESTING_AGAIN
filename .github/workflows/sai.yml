name: My Github actions Cicd Pipeline Testing Again

on:
  push:
    branches: main

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checkout@v4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: build image in docker
        run: docker build -t sairampandu/ss .
      - name: push to  docker hub
        run: docker push sairampandu/ss
      - name: Execute remote SSH commands using password
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          port: ${{ secrets.PORT }}
          script: |
            docker pull sairampandu/ss
            docker stop app || true
            docker rm app || true
            docker run -d -p 80:80 --name app sairampandu/ss
          
